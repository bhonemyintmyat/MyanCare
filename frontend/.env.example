# Copy to .env.production (or set in your host's env settings).
# Swapping from mocks to the real backend is env-only — no code changes:
VITE_USE_MOCKS=false
VITE_API_URL=https://api.your-real-domain.com
